[project]
name = "back-end-python-ebac"
version = "0.1.0"
description = ""
authors = [
    {name = "iuriolv",email = "iurideoliveirasilva@outlook.com"}
]

package-mode = false
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard] (>=0.136.0,<0.137.0)",
    "sqlalchemy (>=2.0.49,<3.0.0)",
    "aiosqlite (>=0.22.1,<0.23.0)",
    "redis (>=8.0.1,<9.0.0)",
    "celery (>=5.6.3,<6.0.0)",
    "kafka-python (>=3.0.8,<4.0.0)",
    "pytest (>=9.1.1,<10.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.init_options]
pythonpath = ["."]