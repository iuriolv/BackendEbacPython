apiVersion: v1
kind: Service
metadata:
  name: backend-ebac-python-service
spec:
  type: ClusterIP
  selector:
    app: fastapi
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8000